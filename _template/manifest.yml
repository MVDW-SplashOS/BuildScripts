# Basic package metadata
package: "example"
version: "1.0.0"

# Dependency
dependency:
  list:
    - "Sample"
  packages:
    Sample:
      version: "1.0.0"

# Build information
build:
  1:
    script: "example-pass1.sh"
    patch: false

