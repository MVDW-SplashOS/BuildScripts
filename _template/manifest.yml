# Basic package metadata
package: "example"
version: "1.0.0"

# Dependency
dependency:
  list:
    - "Sample"
  packages:
    Sample:
      version: "1.0.0"

# Build information
build:
  1:
    enabled: true
    priority: 0
    script: "build.sh"
    patch: false

