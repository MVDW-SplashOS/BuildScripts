# Basic package metadata
package: "Findutils"
version: "4.9.0"

# Build information
build:
  1:
    script: "findutils-pass1.sh"
    patch: false
  2:
    script: "findutils-pass2.sh"
    patch: false

